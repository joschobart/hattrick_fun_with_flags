[project]
name = "hattrick-fwf"
version = "0.1.1"
description = "Add your description here"
dependencies = [
    "flask>=3.0.0",
    "hattrick-libs==0.4.12",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true
